# BUCAD 创新性重构与多数据集验证方案

> 目标：将现有的工程化 BUCAD 主线重构为一项围绕“ROI 可靠性”展开、可证伪、可复现的方法研究。
> 更新日期：2026-08-06

---

## 一、结论：当前创新点确实偏弱，改法要收束

当前 BUCAD 由 ConvNeXt-Tiny、EfficientNetV2-S、UNet-ResNet18、OOF Logistic Stacker、ROI 面积门控和 Grad-CAM 组成。各模块有清晰的工程收益，当前主线在 BUSI 上达到 AUC 0.9256；但它们大多属于既有组件和既有训练策略。若将每个组件分别包装为创新点，论文贡献会显得分散，也难以回答“为何此设计能解决乳腺超声特有问题”。

建议将工作收束为一个中心命题：

> **低对比度、斑点噪声和边界模糊会使预测 ROI 失真；当 ROI 不可靠时，模型应依据可校准的可靠性证据连续决定 ROI 分支对最终诊断的贡献。**

拟议方法命名为 **BRIDGE-BUS**（**B**oundary-aware **R**eliability-Integrated **D**iagnosis with **G**eneralizable **E**vidence for **B**reast **U**ltra**s**ound）。名称可在投稿前调整，方法逻辑必须保持单一闭环：`边界质量提升 -> ROI 可靠性估计 -> 连续诊断路由`。

---

## 二、补充数据集：优先引入真正独立的乳腺超声队列

| 数据集 | 已核验规模与标签 | 推荐角色 | 使用边界 |
|---|---|---|---|
| BUS-BRA | 1,875 张图像、1,064 名患者；病理与 BI-RADS 信息；本地已有 image-mask 对 | 主开发集，病例级五折 OOF | 用于训练、消融、校准器拟合和超参数选择 |
| BUS-UCLM | 683 张图像、38 名患者；174 良性、90 恶性、419 正常；RGB 病灶 mask | 辅助分割预训练 | 同一患者帧高度相关；必须患者级切分，不能把图像随机分到训练和验证 |
| BrEaST / TCIA Breast-Lesions-USG | 256 名患者、266 个病灶；良恶性、病理、BI-RADS 描述和专家分割；CC BY 4.0 | **首选最终外部盲测集** | 参数、结构、阈值冻结后只评估一次；不参与候选筛选 |
| BUSI | 780 张图像，含良性、恶性、正常类别，病灶样本有 mask | 已有历史外测基准 | 当前项目已多次查看 BUSI 指标。后续研究可作冻结后复核，不能再作为反复调参依据 |
| OASBUD | 200 张图像，良恶性各 100，带病灶标注 | 可选第二外部集 | 获取原始文件、许可和病例标识后再纳入；缺少正常样本 |
| UDIAT Dataset B | 163 张图像，109 良性、54 恶性，带分割标注 | 备选小样本外测 | 文献说明通常需联系作者获取；不应依赖镜像站或来源不明的二次打包版本 |

数据来源：BUS-BRA [PubMed](https://pubmed.ncbi.nlm.nih.gov/37937827/)；BUS-UCLM [Mendeley Data](https://data.mendeley.com/datasets/7fvgj4jsp7/3)；BrEaST [TCIA](https://www.cancerimagingarchive.net/collection/breast-lesions-usg)；BUSI 原始论文 DOI [10.1016/j.dib.2019.104863](https://doi.org/10.1016/j.dib.2019.104863)。OASBUD、UDIAT 的数量与可用性参考 [BUS-UCLM 数据论文](https://www.nature.com/articles/s41597-025-04562-3)。

### 推荐数据协议

1. 使用 BUS-UCLM 进行分割器预训练，再在 BUS-BRA 上训练和开发。每一步都以患者或病例为分组单位。
2. 在 BUS-BRA 上执行嵌套病例级五折：内层完成模型选择和校准器训练，外层产生完全 OOF 的预测、mask 和可靠性特征。
3. 冻结全部代码、预处理、权重、融合器和诊断阈值后，在 BrEaST 上做第一次最终盲测；随后在 BUSI 上做历史基准复核。
4. BUSI、BrEaST 的标签、概率、指标和失败病例均不得用于结构选择、门控阈值搜索、伪标签微调或早停。

这一协议解决当前方案最严重的风险：在外部集上伪标签微调会使外部泛化结论失效。

---

## 三、拟议方法论：边界感知的可靠性连续路由

### 3.1 模块 A：边界-频率协同病灶分割器

以现有 UNet-ResNet18 为可复现基线。在解码器末端增加轻量 **Boundary-Frequency Refinement (BFR)** 模块：

- 低频路径保留病灶整体形态与上下文语义；
- 高频路径从浅层特征和边缘响应中提取轮廓细节，抑制超声斑点造成的伪边界；
- 交叉门控融合两类特征，输出 mask 和边界概率图；
- 训练损失为 `Dice + BCE + lambda_b * BoundaryLoss`，边界标签由人工 mask 的形态学边界生成。

此模块要回答一个明确问题：在边界模糊和低对比度病例中，是否能稳定提高病灶轮廓质量，并减少会误导 ROI 分类的异常 mask？

### 3.2 模块 B：OOF 校准的 ROI 可靠性估计器

现有面积门控仅根据 `0.08 <= area_ratio <= 0.75` 做通过或回退。新方法将每个预测 mask 表示为可靠性向量：

`q = [面积比, mask 熵, 最大连通域占比, 边界置信度, 形状紧致度, ROI 与全图预测分歧]`

在 BUS-BRA 的外层 OOF 结果上，为每个病例构造监督目标：ROI 分支相对完整图分支是否降低真实标签的交叉熵。小型校准器 `R(q)` 输出连续可靠性 `alpha in [0, 1]`，最终融合为：

`z_final = alpha * z_roi + (1 - alpha) * z_full`

其中 `z_roi`、`z_full` 为冻结分类器输出的 logit。训练校准器只使用 OOF 预测，避免同一病例同时参与分类器拟合和路由器学习。可靠性还应采用 Brier score、ECE 和可靠性图验证校准程度。

### 3.3 统一的方法链

```text
乳腺超声图像
  -> BFR 分割器输出 mask、边界图和不确定性
  -> 生成 ROI 与可靠性向量 q
  -> 完整图/ROI 双视图分类器输出 z_full、z_roi
  -> OOF 校准器预测 alpha
  -> 连续路由得到 z_final、病灶 mask 和可靠性说明
```

分类器可沿用当前双模型集成，以控制改动范围。论文的方法贡献集中在 BFR 与可靠性路由的因果关联，不把 ConvNeXt、EfficientNet、Grad-CAM 或普通 Logistic Regression 单独包装为新方法。

---

## 四、可写入论文的创新点

1. **面向 BUS 边界失真的边界-频率协同分割。** BFR 同时利用低频形态语义和高频轮廓证据，服务于后续诊断 ROI 的稳定生成。
2. **从硬面积阈值到 OOF 校准的连续 ROI 可靠性路由。** 路由器学习“何时信任 ROI”，以不确定性、连通性、边界质量和跨视图分歧共同决定 ROI 分支权重。
3. **跨队列、零调参的泛化验证。** 将 BrEaST 作为最终盲测，并在 BUSI 上进行冻结后复核，证明方法对设备、中心和标注风格变化的稳健性。

第三项是验证贡献，前两项才是方法贡献。论文中应避免使用“首创”“本质提升”“足以回应批评”这类没有实验支持的表述。

---

## 五、实验设计与判定标准

| 组别 | 目的 |
|---|---|
| E0：现有 UNet + 硬面积门控 | 冻结工程基线 |
| E1：BFR 分割 + 硬面积门控 | 测试边界分割本身的价值 |
| E2：现有 UNet + 连续可靠性路由 | 测试路由机制本身的价值 |
| E3：BFR 分割 + 连续可靠性路由 | 验证完整方法及两模块协同 |
| E4：Oracle mask + 连续可靠性路由 | 给出 ROI 上限，判断瓶颈来自分割还是分类 |

报告分割 Dice、IoU、Boundary-F1；诊断 AUC、Sensitivity、Specificity、F1、Brier、ECE；并采用患者级 bootstrap 95% CI 与配对检验。除总体指标外，单列小病灶、低边界对比度、异常面积和多连通域病例的结果。只有 E3 在 BUS-BRA OOF 与冻结外部集上均超过 E0，才能支撑完整方法的创新性主张。

---

## 六、对原 6 条建议的处理

| 原建议 | 处理 |
|---|---|
| 自研 BUS 分割模块 | 保留，落实为 BFR，并用 Boundary-F1 验证 |
| 分类分割端到端多任务 | 暂缓。该路线需要重新平衡任务损失，且会稀释“可靠 ROI”主题 |
| 不确定性替代面积门控 | 保留，升级为 OOF 校准的连续可靠性路由 |
| 功能解耦双骨干 | 作为后续消融，不写入主创新 |
| CLIP 语义对齐 | 暂缓。缺少匹配的 BI-RADS 文本标注与临床验证，容易形成展示性功能 |
| 半监督 / BUSI 伪标签微调 | 删除 BUSI 微调建议。无标签辅助数据可用于预训练，外部测试集必须保持封存 |

## 七、简述

BUCAD 接下来的工作重点应从“再加一个模型模块”转为“证明模型在 ROI 失真时会做出可靠的证据路由”。用 BUS-UCLM 增加分割训练多样性，用 BUS-BRA 完成严格 OOF 开发，用 BrEaST 与 BUSI 进行冻结外测。这样可以将原先松散的分割、ROI 和融合流程组织成一个围绕超声病灶边界不确定性的完整方法论。
