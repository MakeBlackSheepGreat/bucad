# 开发手册 V2 · 新手从零动手做这个赛题

**项目**：基于超声影像的乳腺肿瘤良恶性分类辅助诊断系统设计（BUCAD）
**读者**：团队 5 人，以前都没接触过医学影像、大多数是 Python 入门
**目标**：跟着这本手册从零把系统做出来，能跑、能看、能演示
**版本**：v2.0（2026-04-23）

> ⚠️ **怎么用这本手册**
> - 从第一章开始**依次**往下做，**不要跳**。
> - 每一步都是"**目标 → 装什么 → 抄什么代码 → 怎么验证做对了 → 常见坑**"。
> - 看到带 `灰底` 的代码块就复制粘贴。命令行里 `$` 是提示符，不要把 `$` 也复制进去。
> - 遇到报错先看本章最后的"常见坑"，再谷歌错误英文。
> - **本手册所有命令和路径中的环境名统一为 `bucad`**，请勿使用其他名称。

---

## 第 0 章 · 读这本手册前你要知道的

### 0.1 这个系统到底要做什么（一句话版本）

**给一张乳腺超声图，告诉医生"这里是肿瘤（画出来），而且可能是良性/恶性（给概率）"。**

### 0.2 我们要做的三件事

1. **分类**：输入 1 张图，输出 良性概率 / 恶性概率。这是核心。
2. **分割**：输入 1 张图，输出一张黑白图（白色就是肿瘤位置）。这是加分。
3. **软件界面**：点按钮 → 选图 → 看结果。没有界面 20 分软件分丢光。

### 0.3 为什么不自己画神经网络

你**不需要**自己写神经网络。社区已经写好几十种成熟模型，我们直接拿来用。**我们要做的是"用好"，不是"发明"**。

### 0.4 数据你手上已经有

- **训练集**（教模型学习）：`训练集/BUSBRA/`，1875 张图 + 1875 张掩膜 + CSV
- **测试集**（给模型打分）：`测试集/Dataset_BUSI_with_GT/`，647 张图（437 良 + 210 恶）
- ❌ 测试集**绝对不能**参与训练（否则等于考试偷看答案，评委一眼就看穿）。

### 0.5 5 分钟医学影像速成

| 概念 | 说白了就是 |
| --- | --- |
| 超声影像 | 一张灰度图，黑白的。不是 CT，不是 X 光。 |
| 病灶 / 肿瘤 | 图里那个椭圆形暗块。良恶性肉眼难分。 |
| 掩膜（Mask） | 一张只有黑白两色的图：白色=肿瘤位置，黑色=背景。医生画的"答案"。 |
| 良性（benign） | 不是癌。纤维腺瘤、囊肿等。 |
| 恶性（malignant） | 癌症。浸润性导管癌等。 |
| BI-RADS | 放射科医生给的"可疑程度"评分，2–5 分。2≈良性，5≈高度可疑恶性。 |
| AUC | 模型好坏的分数，0~1，越大越好，我们要>0.75，目标0.85+。 |
| Sensitivity（敏感性） | **恶性病人**中，模型认出来的比例。**这个最重要**，漏一个恶性=漏诊一个癌症病人。 |
| Specificity（特异性） | **良性病人**中，模型正确放行的比例。 |

### 0.6 什么是 conda 环境（给完全零基础的人）

你可以把 conda 环境想象成一个**独立的小房间**。在这个房间里，Python 和所有库的版本都是固定的，不会影响你电脑上其他项目。我们给这个项目建了一个叫 `bucad` 的房间，所有代码都在这个房间里跑。

**为什么要这样？** 因为不同项目可能需要不同版本的库。比如 A 项目需要 PyTorch 2.0，B 项目需要 PyTorch 2.5，如果装在一起就会打架。conda 环境让它们各住各的。

### 0.7 开工前先做这件事（最容易被忽略）

打开资源管理器进入 `C:\Users\876762330\Desktop\医学影像\`：

- 看到 `训练集.zip` 和 `测试集.zip` 了吗？**右键 → 解压到当前文件夹**。
- 解压完你会多出两个文件夹：`训练集\BUSBRA\` 和 `测试集\Dataset_BUSI_with_GT\`。
- **如果已经有这两个文件夹了**（里面有 `Images`、`Masks`、`benign` 等子目录），跳过这步。

验证一下：

```
$ dir "C:\Users\876762330\Desktop\医学影像\训练集\BUSBRA\Images" | find /c "bus_"
```

应该输出一个大于 1000 的数字（实际是 1875）。数字对就说明数据解压好了。

---

## 第 1 章 · 装环境（W1 Day 1，3 小时）

**目标**：装好 Python、VS Code、PyTorch（能用 GPU）、Git，并验证显卡能训练。

**这一步只有 `刘久华` 和 `杨智杰` 必须亲自装**（他们要写代码）。其他队员可以只装到第 1.4 节，后面能看代码就行。

### 1.1 装 Miniconda（Python 环境管理器）

为什么用 Miniconda 不用官网 Python：**一条命令建多个隔离环境，不会把系统搞乱**。

1. 访问 https://docs.conda.io/en/latest/miniconda.html ，下载 **Windows 64-bit Python 3.10** 安装包（`.exe`）。
2. 双击安装。安装时勾选：
   - ✅ `Add Miniconda3 to my PATH environment variable`（不勾等于白装）
   - ✅ `Register Miniconda3 as my default Python`
3. 安装完毕，**重启电脑**。
4. 按 `Win+R`，输入 `powershell`，回车。在弹出的黑窗口里输入：

```
$ conda --version
```

看到 `conda 23.x.x` 或类似字样 = 成功。

> 💡 **新手提示**：如果你看到 `conda: command not found` 或 `'conda' 不是内部或外部命令`，说明安装时没勾选"Add to PATH"。解决方法：卸载 Miniconda，重新安装，**这次一定勾上那个选项**。

### 1.2 装 VS Code（编辑器）

1. 访问 https://code.visualstudio.com/ ，下载 Windows 版。
2. 安装时勾选：
   - ✅ `Add to PATH`
   - ✅ `Register Code as an editor for supported file types`
3. 打开 VS Code，左侧第 5 个图标（插件市场）里装：
   - **Python**（Microsoft 官方）
   - **Jupyter**（Microsoft 官方）
   - **Pylance**（Microsoft 官方）
   - **Chinese (Simplified) Language Pack**（中文界面）

### 1.3 装 Git 和 GitHub Desktop

1. 装 Git：https://git-scm.com/download/win 下载并默认安装。
2. 装 GitHub Desktop：https://desktop.github.com/ （图形界面，新手友好）。
3. 在 GitHub.com 上注册账号。队长 `朱笑菲` 创建一个**私有仓库**，命名建议 `bucad`，然后把队员邀请进来（Settings → Collaborators）。

### 1.4 建项目文件夹

**约定好**：所有代码放在 `C:\Users\876762330\Desktop\医学影像\code\` 下。**绝对不要**把代码和数据放同一个目录，否则 git 会想把数据也传上去（数据几十 G，传不动）。

```
$ cd "C:\Users\876762330\Desktop\医学影像"
$ mkdir code
$ cd code
```

### 1.5 建 conda 环境（这是关键）

**重要**：每个人都要照这个装，这样结果才一致。**项目环境名统一叫 `bucad`**（Breast Ultrasound Computer-Aided Diagnosis 的缩写）。

```
$ conda create -n bucad python=3.10 -y
$ conda activate bucad
```

看到命令行左边出现 `(bucad)` 就对了。

> 💡 **新手提示**：每次打开新的命令行窗口，都要先跑 `conda activate bucad` 进入我们的项目环境。如果忘了激活，装的库会装到别的地方去，代码就找不到它们了。**养成习惯：开终端第一件事就是 `conda activate bucad`**。

> 💡 **怎么确认自己在 bucad 环境里？** 看命令行最左边，应该有 `(bucad)` 字样。如果没有，就跑 `conda activate bucad`。

### 1.6 装 PyTorch（GPU 版）

**先确认你的显卡是 NVIDIA**：按 `Ctrl+Shift+Esc` 打开任务管理器 → 性能 → 左侧应该能看到 "GPU 0 NVIDIA ..."。

**查你的 CUDA 版本**：

```
$ nvidia-smi
```

看右上角 `CUDA Version: 13.x`（或 12.x）。

> 🚧 **如果 `nvidia-smi` 报 "not recognized" / "不是内部或外部命令"**
> 说明你连 NVIDIA 驱动都没装。去 https://www.nvidia.com/Download/index.aspx 按你的显卡型号下载"Game Ready Driver"并安装，重启后再来。
>
> **如果你的笔记本根本没 NVIDIA 显卡**（只有 Intel/AMD 核显），跳过 1.6，直接装 CPU 版 PyTorch：`pip install torch torchvision`。训练会慢 10 倍，先在本地跑通 `--epochs 2` 验证流程正确，剩下用 Colab/Kaggle 白嫖 GPU 跑（群里找刘久华要托管方案）。

**装 PyTorch**（以 CUDA 13.0 为例）：

```
$ pip install torch torchvision --index-url https://download.pytorch.org/whl/cu130
```

> 💡 **如果你的 CUDA 版本是 12.x**：把上面命令里的 `cu130` 换成 `cu126`（对应 CUDA 12.6）或 `cu128`（对应 CUDA 12.8）。**版本必须匹配**，不然 GPU 用不了。
>
> 💡 **怎么确认 CUDA 版本？** 跑 `nvidia-smi`，看右上角写的 `CUDA Version: xx.x`，取整数部分就是你的 CUDA 主版本。

### 1.7 装其他库（一次性装完）

**把下面这段保存为 `C:\Users\876762330\Desktop\医学影像\code\requirements.txt`**：

```
numpy
pandas
opencv-python
Pillow
scikit-learn
scikit-image
matplotlib
seaborn
tqdm
pyyaml
timm
segmentation_models_pytorch
albumentations
monai
tensorboard
grad-cam
gradio
pyinstaller
jupyter
```

然后在 `(bucad)` 环境里装：

```
$ cd "C:\Users\876762330\Desktop\医学影像\code"
$ pip install -r requirements.txt
```

等 3~5 分钟。

> 💡 **pip 超时怎么办？** 国内网络经常下载慢。用清华镜像源加速：
> ```
> $ pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
> ```

### 1.8 验证 GPU 能用

在 `code/` 下新建 `check_env.py`，内容：

```python
import torch
print('torch version:', torch.__version__)
print('cuda available:', torch.cuda.is_available())
print('device count:', torch.cuda.device_count())
if torch.cuda.is_available():
    print('device name:', torch.cuda.get_device_name(0))
    x = torch.rand(3, 3).cuda()
    print('tensor on gpu ok:', x.device)
```

运行：

```
$ python check_env.py
```

**期望输出**：
```
torch version: 2.x.x+cu130
cuda available: True
device count: 1
device name: NVIDIA GeForce RTX xxxx
tensor on gpu ok: cuda:0
```

看到 `cuda available: True` = 成功。如果是 `False`，去看本章末尾的"常见坑 C1"。

### 1.8+ 体检脚本：一次看全部库都装对没

每次装完环境或换机器，跑一下这个，省得一个个查。新建 `check_all.py`：

```python
"""一次性检查所有关键库是否就绪。"""
import importlib, sys

checks = [
    ('torch', 'torch.__version__'),
    ('torchvision', 'torchvision.__version__'),
    ('timm', 'timm.__version__'),
    ('segmentation_models_pytorch', 'segmentation_models_pytorch.__version__'),
    ('cv2', 'cv2.__version__'),
    ('numpy', 'numpy.__version__'),
    ('pandas', 'pandas.__version__'),
    ('sklearn', 'sklearn.__version__'),
    ('matplotlib', 'matplotlib.__version__'),
    ('gradio', 'gradio.__version__'),
    ('pytorch_grad_cam', None),
    ('tqdm', 'tqdm.__version__'),
]
fail = 0
for name, ver_expr in checks:
    try:
        m = importlib.import_module(name)
        ver = eval(ver_expr) if ver_expr else '(ok)'
        print(f'[OK]  {name:<30} {ver}')
    except Exception as e:
        print(f'[FAIL]{name:<30} -> {e}')
        fail += 1
import torch
print(f'\ncuda available: {torch.cuda.is_available()}')
if torch.cuda.is_available():
    print(f'gpu: {torch.cuda.get_device_name(0)}')
sys.exit(fail)
```

跑：

```
$ python check_all.py
```

全部 `[OK]` + `cuda available: True` 就能开工。任何一行 `[FAIL]` 都回第 1.7 节重装对应的包。

### 1.9 常见坑

- **C1：`cuda available: False`**：PyTorch 装的是 CPU 版。卸载重装：`pip uninstall torch torchvision`，然后严格按 1.6 节带 `--index-url` 装。
- **C2：pip 超时**：换清华源：`pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple`。
- **C3：`conda: command not found`**：PATH 没装好，重装 Miniconda 时勾上"Add to PATH"。
- **C4：中文路径乱码**：命令行在中文路径下输中文可能出错，全部用双引号包起来：`cd "C:\Users\876762330\Desktop\医学影像\code"`。
- **C5：`(bucad)` 没出现**：你忘了跑 `conda activate bucad`。每次开新终端都要跑一次。

---

## 第 2 章 · 先用眼睛看懂数据（W1 Day 2 上午，2 小时）

**目标**：在不写训练代码之前，先亲眼看看每张图什么样、掩膜怎么和图对齐、CSV 里写了啥。没看过数据就写代码＝瞎做。

### 2.1 直接双击打开几张图

1. 资源管理器打开 `训练集/BUSBRA/Images/`
2. 双击 `bus_0001-l.png` ← 这是第 1 号病例左切面。
3. 再打开 `训练集/BUSBRA/Masks/mask_0001-l.png` ← 这就是"答案图"，白色块就是肿瘤。
4. 比较几个不同样本：`bus_0002-l.png`（良性）、`bus_0100-l.png`、`bus_0500-l.png`。

**你应该看到**：
- 超声图是**灰度图**（黑白）；
- 掩膜是**黑底 + 白色形状**（白色 = 肿瘤，黑色 = 背景）；
- 不同设备、不同病人，图的大小、亮度都不一样。

### 2.2 打开 CSV 看标签

用 **Excel** 打开 `训练集/BUSBRA/bus_data.csv`，你会看到：

| ID | Case | Histology | Pathology | BIRADS | Device | Width | Height | Side | BBOX |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| bus_0001-l | 1 | invasive ductal carcinoma | malignant | 4 | GE Logiq 7 | 274 | 353 | left | [91,24,103,79] |

记住这几件事：
- `ID` = 去掉 `.png` 的文件名。
- `Pathology` 就是我们要预测的 **benign / malignant**。
- `Case` 是病人编号。**同一个 Case 有 `-l` 和 `-r` 两张图是同一个人的不同切面**，不能让 `-l` 进训练、`-r` 进验证。
- `BBOX = [x, y, 宽, 高]`，肿瘤的矩形框。

### 2.3 用 Python 读一张图（动手练）

在 `code/` 下新建 `view_data.py`：

```python
import cv2
import matplotlib.pyplot as plt

# ⚠️ Windows 下 matplotlib 默认字体不含中文，要先指定一下
plt.rcParams['font.sans-serif'] = ['SimHei', 'Microsoft YaHei']
plt.rcParams['axes.unicode_minus'] = False

# 路径注意用原始字符串 r"..."，否则 \ 会被转义
img_path = r"C:\Users\876762330\Desktop\医学影像\训练集\BUSBRA\Images\bus_0001-l.png"
msk_path = r"C:\Users\876762330\Desktop\医学影像\训练集\BUSBRA\Masks\mask_0001-l.png"

img = cv2.imread(img_path, cv2.IMREAD_GRAYSCALE)
msk = cv2.imread(msk_path, cv2.IMREAD_GRAYSCALE)

# 读出来如果是 None 就是路径错了，先检查
assert img is not None, f'读不到图：{img_path}'
assert msk is not None, f'读不到掩膜：{msk_path}'

print('图像 shape:', img.shape, 'dtype:', img.dtype)
print('掩膜 shape:', msk.shape, '唯一取值:', set(msk.flatten().tolist()[:1000]))

fig, axes = plt.subplots(1, 3, figsize=(12, 4))
axes[0].imshow(img, cmap='gray'); axes[0].set_title('原图'); axes[0].axis('off')
axes[1].imshow(msk, cmap='gray'); axes[1].set_title('掩膜'); axes[1].axis('off')
axes[2].imshow(img, cmap='gray')
axes[2].imshow(msk, cmap='Reds', alpha=0.4)  # 半透明红色叠加
axes[2].set_title('叠加'); axes[2].axis('off')
plt.tight_layout(); plt.show()
```

运行：

```
$ python view_data.py
```

**你应该看到**：三张图并排，最右边那张是原图 + 红色半透明的肿瘤区域。

### 2.4 自测

- [ ] 你能不能告诉队友 `bus_0010-l.png` 和 `bus_0010-r.png` 是不是同一个病人？（答：是）
- [ ] CSV 里 `Pathology` 有几种取值？（答：2 种：benign 和 malignant）
- [ ] `normal/` 子目录要不要用？（答：不用，赛题明令排除）

---

## 第 3 章 · 建仓库，写第一个脚本（W1 Day 2 下午，2 小时）

### 3.1 建仓库结构

在 `code/` 下建：

```
code/
├── README.md
├── requirements.txt          ← 已有（第 1.7 节）
├── check_env.py              ← 已有（第 1.8 节）
├── view_data.py              ← 已有（第 2.3 节）
├── configs/                  ← 配置 YAML
├── src/
│   ├── __init__.py
│   ├── datasets/
│   ├── models/
│   ├── preprocess/
│   ├── engine/
│   ├── explain/
│   └── utils/
├── scripts/                  ← 一键跑的脚本
├── app/                      ← Gradio 界面
├── tests/                    ← 单元测试
└── artifacts/                ← 权重、日志、报告
    ├── checkpoints/
    ├── logs/
    └── reports/
```

批量创建命令：

> ⚠️ **这一段必须在 cmd 里跑，不要在 PowerShell 里跑**（`type nul >` 是 cmd 语法）。
> 开一个新 cmd 的方法：按 `Win+R`，输入 `cmd`，回车。
> **记得先进入 conda 环境**：`conda activate bucad`

```
$ cd /d "C:\Users\876762330\Desktop\医学影像\code"
$ mkdir configs src src\datasets src\models src\preprocess src\engine src\explain src\utils scripts app tests artifacts artifacts\checkpoints artifacts\logs artifacts\reports
$ type nul > src\__init__.py
$ type nul > src\datasets\__init__.py
$ type nul > src\models\__init__.py
$ type nul > src\preprocess\__init__.py
$ type nul > src\engine\__init__.py
$ type nul > src\explain\__init__.py
$ type nul > src\utils\__init__.py
```

> 💡 **为什么要那么多 `__init__.py`**：这是 Python 识别"文件夹是一个模块"的信号。空文件就够了。

### 3.2 建 `.gitignore`（**极其重要**，不然会把几个 G 的数据推到 GitHub 害死网速）

在 `code/` 下新建文件 `.gitignore`，内容：

```
# 数据（绝对不要传）
data/
*.png
*.jpg
*.dcm
*.nii
*.npz

# 模型权重太大
*.pt
*.pth
*.ckpt
artifacts/checkpoints/

# Python 缓存
__pycache__/
*.pyc
.ipynb_checkpoints/

# 本地环境
.env
.vscode/
.idea/

# 日志
artifacts/logs/
*.log

# 系统
Thumbs.db
.DS_Store
```

### 3.3 第一次 commit

用 **GitHub Desktop**：
1. File → Add local repository → 选 `code/` 文件夹
2. 底部写 commit 描述 "chore: 仓库初始化"
3. 点 `Commit to main`
4. 点右上角 `Publish repository`（选 Private）

**以后每写完一段就 commit 一次**。不要攒到最后一起 commit。

### 3.4 常见坑

- **"git 传不动"**：99% 是 `.gitignore` 没生效，数据被加进去了。用 GitHub Desktop 看一下 Changes 面板是不是有 `.png` 文件，有的话去 `.gitignore` 补规则。
- **`PermissionError`**：VS Code 打开了同一个文件，关掉再试。

---

## 第 4 章 · 写数据集加载器（W1 Day 3，半天）

**目标**：写一个 `Dataset` 类，每次给它一个数字，它吐出"一张图+它的标签"。这是 PyTorch 的标准套路。

### 4.1 PyTorch Dataset 是什么

把它想象成**一本菜单**：菜单上有 1875 道菜，你说"给我第 7 道"，它就给你做出来。模型训练时会"点 1875 次"。

### 4.2 写 BUSBRA Dataset（完整代码，直接复制）

新建 `src/datasets/busbra.py`：

```python
from pathlib import Path
import pandas as pd
import numpy as np
import cv2
from torch.utils.data import Dataset


class BUSBRADataset(Dataset):
    """
    BUSBRA 训练集。
    返回 (image_tensor, label_int, mask_tensor_or_None)。
    label: benign=0, malignant=1
    """

    LABEL_MAP = {'benign': 0, 'malignant': 1}

    def __init__(self, root, ids, csv_path=None, img_size=224,
                 return_mask=False, transform=None):
        """
        root: BUSBRA 根目录
        ids: 要用的图像 ID 列表（从 artifacts/splits/foldN_*.txt 读出来）
        img_size: 统一缩放到多少像素，默认 224（ImageNet 预训练的标准）
        return_mask: 是否同时返回掩膜（分类训练=False，分割训练=True）
        transform: 可选的 albumentations 增强。不用就填 None。
        """
        self.root = Path(root)
        self.img_dir = self.root / 'Images'
        self.msk_dir = self.root / 'Masks'
        if csv_path is None:
            csv_path = self.root / 'bus_data.csv'
        df = pd.read_csv(csv_path)
        df = df[df['ID'].isin(ids)].reset_index(drop=True)
        self.df = df
        self.img_size = img_size
        self.return_mask = return_mask
        self.transform = transform

    def __len__(self):
        return len(self.df)

    def __getitem__(self, idx):
        row = self.df.iloc[idx]
        img_id = row['ID']
        label = self.LABEL_MAP[row['Pathology']]

        img = cv2.imread(str(self.img_dir / f'{img_id}.png'), cv2.IMREAD_GRAYSCALE)
        # 图像命名 bus_XXXX-s.png，掩膜命名 mask_XXXX-s.png，把前缀换掉
        mask_name = img_id.replace('bus_', 'mask_') + '.png'
        msk = cv2.imread(str(self.msk_dir / mask_name), cv2.IMREAD_GRAYSCALE)

        img = cv2.resize(img, (self.img_size, self.img_size))
        msk = cv2.resize(msk, (self.img_size, self.img_size), interpolation=cv2.INTER_NEAREST)

        # 单通道 → 三通道（给预训练模型用）
        img = cv2.cvtColor(img, cv2.COLOR_GRAY2RGB)

        if self.transform is not None:
            res = self.transform(image=img, mask=msk)
            img, msk = res['image'], res['mask']

        # 变成 PyTorch 张量 (C,H,W)，像素归一到 0~1
        img_t = img.astype(np.float32).transpose(2, 0, 1) / 255.0
        msk_t = (msk > 127).astype(np.float32)  # 二值化

        sample = {'image': img_t, 'label': label, 'id': img_id}
        if self.return_mask:
            sample['mask'] = msk_t
        return sample
```

### 4.3 写 BUSI Dataset（测试集，结构不同）

新建 `src/datasets/busi.py`：

```python
from pathlib import Path
import numpy as np
import cv2
from torch.utils.data import Dataset


class BUSIDataset(Dataset):
    """
    BUSI 外部测试集。只用 benign + malignant。
    自动扫描目录，不依赖 CSV。
    """

    LABEL_MAP = {'benign': 0, 'malignant': 1}

    def __init__(self, root, img_size=224, return_mask=False):
        self.root = Path(root)
        self.img_size = img_size
        self.return_mask = return_mask
        self.items = []
        for cls in ['benign', 'malignant']:
            cls_dir = self.root / cls
            for p in sorted(cls_dir.glob('*.png')):
                if '_mask' in p.name:
                    continue  # 跳过掩膜文件
                self.items.append((p, cls))

    def __len__(self):
        return len(self.items)

    def _load_mask(self, img_path):
        """BUSI 掩膜命名：benign (1)_mask.png；多病灶还有 _mask_1.png 等，要合并。"""
        stem = img_path.stem  # 'benign (1)'
        masks = list(img_path.parent.glob(f'{stem}_mask*.png'))
        if not masks:
            return None
        acc = None
        for m in masks:
            mk = cv2.imread(str(m), cv2.IMREAD_GRAYSCALE)
            acc = mk if acc is None else np.maximum(acc, mk)
        return acc

    def __getitem__(self, idx):
        img_path, cls = self.items[idx]
        label = self.LABEL_MAP[cls]
        img = cv2.imread(str(img_path), cv2.IMREAD_GRAYSCALE)
        img = cv2.resize(img, (self.img_size, self.img_size))
        img = cv2.cvtColor(img, cv2.COLOR_GRAY2RGB)
        img_t = img.astype(np.float32).transpose(2, 0, 1) / 255.0

        sample = {'image': img_t, 'label': label, 'id': img_path.stem}
        if self.return_mask:
            msk = self._load_mask(img_path)
            if msk is None:
                msk = np.zeros((self.img_size, self.img_size), dtype=np.float32)
            else:
                msk = cv2.resize(msk, (self.img_size, self.img_size), interpolation=cv2.INTER_NEAREST)
                msk = (msk > 127).astype(np.float32)
            sample['mask'] = msk
        return sample
```

### 4.4 Case 级分层 split（防止数据泄漏）

新建 `scripts/make_split.py`：

```python
"""生成 Case 级 5-fold 划分。输出到 artifacts/splits/。"""
import pandas as pd
from pathlib import Path

BUSBRA_ROOT = Path(r"C:\Users\876762330\Desktop\医学影像\训练集\BUSBRA")
OUT_DIR = Path(r"C:\Users\876762330\Desktop\医学影像\code\artifacts\splits")
OUT_DIR.mkdir(parents=True, exist_ok=True)

# 直接用 BUSBRA 官方 5-fold，它已经是 Case 级分层
df = pd.read_csv(BUSBRA_ROOT / '5-fold-cv.csv')
print('总样本:', len(df))
print('kFold 分布:\n', df['kFold'].value_counts().sort_index())

for k in range(1, 6):
    val_ids = df[df['kFold'] == k]['ID'].tolist()
    train_ids = df[df['kFold'] != k]['ID'].tolist()
    pd.Series(train_ids).to_csv(OUT_DIR / f'fold{k}_train.txt', index=False, header=False)
    pd.Series(val_ids).to_csv(OUT_DIR / f'fold{k}_val.txt', index=False, header=False)
    # 再做一遍 Case 级不泄漏校验
    train_cases = set(df[df['kFold'] != k]['ID'].str.extract(r'bus_(\d+)')[0])
    val_cases = set(df[df['kFold'] == k]['ID'].str.extract(r'bus_(\d+)')[0])
    overlap = train_cases & val_cases
    assert not overlap, f'fold{k} 有病例泄漏: {overlap}'
    print(f'fold{k}: train={len(train_ids)}, val={len(val_ids)}, 无泄漏')

print('完成。')
```

运行：

```
$ cd "C:\Users\876762330\Desktop\医学影像\code"
$ python scripts\make_split.py
```

### 4.5 自测：从 Dataset 里取一批看看

新建 `scripts/peek_loader.py`：

```python
import sys; sys.path.append('.')  # 让 Python 能找到 src
import matplotlib.pyplot as plt
plt.rcParams['font.sans-serif'] = ['SimHei', 'Microsoft YaHei']
plt.rcParams['axes.unicode_minus'] = False
import pandas as pd
from torch.utils.data import DataLoader
from src.datasets.busbra import BUSBRADataset

ROOT = r"C:\Users\876762330\Desktop\医学影像\训练集\BUSBRA"
ids = pd.read_csv('artifacts/splits/fold1_train.txt', header=None)[0].tolist()
ds = BUSBRADataset(ROOT, ids=ids, img_size=224, return_mask=True)
print('数据集大小:', len(ds))

loader = DataLoader(ds, batch_size=4, shuffle=True)
batch = next(iter(loader))
print('image shape:', batch['image'].shape)
print('labels:', batch['label'].tolist())

fig, axes = plt.subplots(2, 4, figsize=(14, 7))
for i in range(4):
    img = batch['image'][i].permute(1, 2, 0).numpy()
    msk = batch['mask'][i].numpy()
    axes[0, i].imshow(img); axes[0, i].set_title(f"label={batch['label'][i].item()}"); axes[0, i].axis('off')
    axes[1, i].imshow(img); axes[1, i].imshow(msk, cmap='Reds', alpha=0.4); axes[1, i].axis('off')
plt.tight_layout(); plt.show()
```

跑：

```
$ python scripts\peek_loader.py
```

**期望**：弹出一张图，上排 4 张原图，下排是叠加掩膜。label=0 就是良性，1 就是恶性。

### 4.6 常见坑

- **ModuleNotFoundError: No module named 'src'**：你不在 `code/` 目录下运行，或者忘了 `sys.path.append('.')`。
- **PIL/cv2 读不到图**：99% 是路径写错。用 `from pathlib import Path; print(Path(p).exists())` 检查。

---

## 第 5 章 · 训练第一个分类模型（W1 Day 4–5，2 天）

**目标**：用 ResNet-50 跑一个最基础的分类模型，在 BUS-BRA 1 折上跑通训练 + 验证 + 保存权重。

### 5.1 用 timm 拿模型（一行代码）

在 `src/models/classifier.py` 里：

```python
import timm
import torch.nn as nn


def build_classifier(name='resnet50', num_classes=2, pretrained=True):
    """从 timm 取一个预训练分类模型。"""
    model = timm.create_model(name, pretrained=pretrained, num_classes=num_classes)
    return model
```

就这样。`timm` 里有 600+ 个预训练模型随便挑。

### 5.2 训练脚本（一个完整的能跑的脚本）

**这个脚本在干啥**（读代码前先看这张图）：

```
for 每一轮 epoch:
    【训练阶段】对每一批图：前向 → 算损失 → 反传 → 更新权重
    【验证阶段】把模型冻住，跑验证集，算 val AUC
    如果 val AUC 创新高 → 把模型存到磁盘
```

总共 30 轮，存下来的就是"最佳权重"。你不需要懂每行，会改 `--fold` 和 `--epochs` 参数就行。

新建 `scripts/train_cls.py`。**照抄**：

```python
"""
训练 ResNet-50 分类器（单折）。
示例：python scripts\train_cls.py --fold 1 --epochs 30
"""
import argparse, sys, time, os
sys.path.append('.')
from pathlib import Path
import numpy as np, pandas as pd, torch
from torch.utils.data import DataLoader
from torch.optim import AdamW
from torch.optim.lr_scheduler import CosineAnnealingLR
from torch.nn import CrossEntropyLoss
from sklearn.metrics import roc_auc_score
from tqdm import tqdm

from src.datasets.busbra import BUSBRADataset
from src.models.classifier import build_classifier

BUSBRA_ROOT = r"C:\Users\876762330\Desktop\医学影像\训练集\BUSBRA"


def set_seed(s=42):
    import random
    random.seed(s); np.random.seed(s); torch.manual_seed(s); torch.cuda.manual_seed_all(s)


def load_ids(path):
    return pd.read_csv(path, header=None)[0].tolist()


def main():
    ap = argparse.ArgumentParser()
    ap.add_argument('--fold', type=int, default=1)
    ap.add_argument('--model', default='resnet50')
    ap.add_argument('--img_size', type=int, default=224)
    ap.add_argument('--bs', type=int, default=32)
    ap.add_argument('--lr', type=float, default=3e-4)
    ap.add_argument('--epochs', type=int, default=30)
    ap.add_argument('--out', default='artifacts/checkpoints')
    args = ap.parse_args()

    set_seed(42)
    device = 'cuda' if torch.cuda.is_available() else 'cpu'
    print(f'device = {device}')

    train_ids = load_ids(f'artifacts/splits/fold{args.fold}_train.txt')
    val_ids = load_ids(f'artifacts/splits/fold{args.fold}_val.txt')
    print(f'fold{args.fold}: train={len(train_ids)}, val={len(val_ids)}')

    train_ds = BUSBRADataset(BUSBRA_ROOT, train_ids, img_size=args.img_size)
    val_ds = BUSBRADataset(BUSBRA_ROOT, val_ids, img_size=args.img_size)
    # num_workers=0 是 Windows 的硬性要求（多进程会死锁），不要改
    # pin_memory=True 加速 CPU→GPU 拷贝；没有 GPU 时会打一行警告，不影响运行
    pin = torch.cuda.is_available()
    train_loader = DataLoader(train_ds, batch_size=args.bs, shuffle=True, num_workers=0, pin_memory=pin)
    val_loader = DataLoader(val_ds, batch_size=args.bs, shuffle=False, num_workers=0, pin_memory=pin)

    model = build_classifier(args.model).to(device)
    # 类不平衡：良恶比 ≈ 1268:607，恶性权重更大
    weights = torch.tensor([1.0, 2.0], dtype=torch.float32).to(device)
    criterion = CrossEntropyLoss(weight=weights)
    optim = AdamW(model.parameters(), lr=args.lr, weight_decay=1e-4)
    sched = CosineAnnealingLR(optim, T_max=args.epochs, eta_min=1e-6)

    Path(args.out).mkdir(parents=True, exist_ok=True)
    best_auc = 0.0
    for epoch in range(1, args.epochs + 1):
        # ---- train ----
        model.train()
        tr_loss = 0.0
        pbar = tqdm(train_loader, desc=f'epoch {epoch}/{args.epochs} [train]')
        for batch in pbar:
            img = batch['image'].to(device)
            lbl = batch['label'].to(device)
            optim.zero_grad()
            out = model(img)
            loss = criterion(out, lbl)
            loss.backward()
            optim.step()
            tr_loss += loss.item() * img.size(0)
            pbar.set_postfix(loss=f'{loss.item():.4f}')
        tr_loss /= len(train_ds)
        sched.step()

        # ---- val ----
        model.eval()
        probs, labels = [], []
        with torch.no_grad():
            for batch in val_loader:
                img = batch['image'].to(device)
                lbl = batch['label'].numpy()
                out = model(img)
                p = torch.softmax(out, dim=1)[:, 1].cpu().numpy()  # 恶性概率
                probs.extend(p.tolist()); labels.extend(lbl.tolist())
        auc = roc_auc_score(labels, probs)
        print(f'epoch {epoch:02d} | tr_loss={tr_loss:.4f} | val_auc={auc:.4f}')

        if auc > best_auc:
            best_auc = auc
            ckpt = Path(args.out) / f'{args.model}_fold{args.fold}_best.pt'
            torch.save({'model': model.state_dict(),
                        'auc': auc, 'epoch': epoch, 'args': vars(args)}, ckpt)
            print(f'  + 保存最佳权重 auc={auc:.4f} -> {ckpt}')

    print(f'fold{args.fold} 训练完成，最佳 val AUC = {best_auc:.4f}')


if __name__ == '__main__':
    main()
```

### 5.3 跑起来

```
$ python scripts\train_cls.py --fold 1 --epochs 30
```

你会看到：
- 第一次运行会下载预训练权重（大约 100 MB），耐心等。
- 每个 epoch 大约 1–3 分钟（看 GPU）。
- 屏幕滚动，tqdm 进度条往前走。
- 每个 epoch 末尾打印 `val_auc`，期望第 10 个 epoch 就能到 0.85+。

> 🚧 **预训练权重卡在"下载中 0%"怎么办**
> 国内访问 `download.pytorch.org` 经常很慢或 timeout。在**打开 cmd 前**先设置镜像：
> ```
> $ set HF_ENDPOINT=https://hf-mirror.com
> $ set TORCH_HOME=D:\torch_cache
> ```
> 或者从别人那拷一份 `%USERPROFILE%\.cache\torch\hub\checkpoints\` 到你的机器。刘久华下载好就发群里。

### 5.4 跑完 5 折

写个批处理文件 `scripts\train_all_folds.bat`：

```
@echo off
for %%f in (1 2 3 4 5) do (
    python scripts\train_cls.py --fold %%f --epochs 30
)
```

运行：

```
$ scripts\train_all_folds.bat
```

这一步会跑 3~5 个小时。你可以先跑 `--fold 1 --epochs 5` 确认一切正常。

### 5.5 自测

- [ ] `artifacts/checkpoints/` 下应该有 5 个 `.pt` 文件。
- [ ] 每折 best val AUC >= 0.85（平均 0.88+）。

### 5.6 常见坑

- **CUDA out of memory**：把 `--bs 32` 改成 `--bs 16` 或 `--bs 8`。
- **训练变慢 / 爆内存**：关掉浏览器和 VS Code 以外的程序。
- **AUC 一直 0.5**：多半是标签读错了。检查 `BUSBRADataset.LABEL_MAP`，确保 `malignant=1`。
- **训练集图完全找不到**：掩膜文件名是 `mask_XXXX`，不是 `bus_XXXX`。

---

## 第 6 章 · 看模型跑得好不好（W2 Day 1，半天）

### 6.1 写指标计算函数

新建 `src/utils/metrics.py`：

```python
import numpy as np
from sklearn.metrics import (roc_auc_score, accuracy_score, confusion_matrix,
                             precision_score, recall_score, f1_score)


def binary_metrics(y_true, y_prob, threshold=0.5):
    """二分类指标。阳性 = 恶性 = 1。"""
    y_pred = (np.asarray(y_prob) >= threshold).astype(int)
    y_true = np.asarray(y_true).astype(int)
    tn, fp, fn, tp = confusion_matrix(y_true, y_pred, labels=[0, 1]).ravel()
    return {
        'AUC': roc_auc_score(y_true, y_prob),
        'Accuracy': accuracy_score(y_true, y_pred),
        'Sensitivity': recall_score(y_true, y_pred),         # TP/(TP+FN)
        'Specificity': tn / (tn + fp) if (tn + fp) else 0.0,
        'Precision': precision_score(y_true, y_pred, zero_division=0),
        'F1': f1_score(y_true, y_pred, zero_division=0),
        'threshold': threshold,
        'confusion': {'TP': int(tp), 'FP': int(fp), 'TN': int(tn), 'FN': int(fn)},
    }


def best_youden_threshold(y_true, y_prob):
    """找让 Sensitivity + Specificity - 1 最大的阈值。"""
    from sklearn.metrics import roc_curve
    fpr, tpr, thr = roc_curve(y_true, y_prob)
    j = tpr - fpr
    return float(thr[np.argmax(j)])
```

### 6.2 在 BUSI 上跑评测（最终打分用）

新建 `scripts/eval_busi.py`：

```python
import argparse, sys, json
sys.path.append('.')
from pathlib import Path
import torch, numpy as np
from torch.utils.data import DataLoader
from src.datasets.busi import BUSIDataset
from src.models.classifier import build_classifier
from src.utils.metrics import binary_metrics, best_youden_threshold

BUSI_ROOT = r"C:\Users\876762330\Desktop\医学影像\测试集\Dataset_BUSI_with_GT"


def infer(ckpts, ds, device='cuda'):
    loader = DataLoader(ds, batch_size=32, shuffle=False)
    all_probs, all_labels, all_ids = [], [], []
    models = []
    for ck in ckpts:
        state = torch.load(ck, map_location=device)
        model = build_classifier(state['args']['model']).to(device)
        model.load_state_dict(state['model'])
        model.eval()
        models.append(model)

    with torch.no_grad():
        for batch in loader:
            img = batch['image'].to(device)
            prob = torch.zeros(img.size(0), device=device)
            for m in models:
                p = torch.softmax(m(img), dim=1)[:, 1]
                # TTA：水平翻转
                p_flip = torch.softmax(m(torch.flip(img, dims=[-1])), dim=1)[:, 1]
                prob += (p + p_flip) / 2
            prob /= len(models)
            all_probs.extend(prob.cpu().numpy().tolist())
            all_labels.extend(batch['label'].numpy().tolist())
            all_ids.extend(batch['id'])
    return np.array(all_labels), np.array(all_probs), all_ids


def main():
    ap = argparse.ArgumentParser()
    ap.add_argument('--ckpt_dir', default='artifacts/checkpoints')
    ap.add_argument('--pattern', default='resnet50_fold*_best.pt')
    args = ap.parse_args()
    ckpts = sorted(Path(args.ckpt_dir).glob(args.pattern))
    print(f'使用权重: {[c.name for c in ckpts]}')

    device = 'cuda' if torch.cuda.is_available() else 'cpu'
    ds = BUSIDataset(BUSI_ROOT, img_size=224)
    print(f'BUSI 样本数: {len(ds)}（期望 647）')

    y_true, y_prob, ids = infer(ckpts, ds, device)

    # 默认阈值 0.5
    m05 = binary_metrics(y_true, y_prob, threshold=0.5)
    # Youden 最优阈值
    th = best_youden_threshold(y_true, y_prob)
    m_th = binary_metrics(y_true, y_prob, threshold=th)

    report = {'threshold_0.5': m05, 'threshold_youden': m_th}
    out = Path('artifacts/reports/busi_metrics.json')
    out.parent.mkdir(parents=True, exist_ok=True)
    out.write_text(json.dumps(report, indent=2, ensure_ascii=False), encoding='utf-8')
    print(json.dumps(report, indent=2, ensure_ascii=False))
    print(f'\n已保存 -> {out}')


if __name__ == '__main__':
    main()
```

运行：

```
$ python scripts\eval_busi.py
```

你应该看到一个 JSON，里面有 AUC / Accuracy / Sensitivity 等 6 个指标，两个阈值各一份。

### 6.3 自测

- [ ] `BUSI 样本数` 必须是 **647**（437+210）。如果不对，你没排除掉 normal 或路径写错。
- [ ] AUC > 0.75（赛题门槛）。低于就看第 6.4 节。

### 6.4 常见坑 / AUC 不达门槛怎么办

1. 检查 BUSI Dataset 路径，别漏掉掩膜过滤；
2. 确保 5 个权重都在集成里（不是只用 1 个）；
3. 打开 TTA（已在代码里）；
4. 用 Youden 阈值代替 0.5；
5. 仍不够 → 升级到 EfficientNet-B3：`--model efficientnet_b3`。

---

## 第 7 章 · 图像增强（W2 Day 2，半天，提分 2~5 AUC）

**为什么**：超声图光照差异大，病灶边界模糊。加 CLAHE（局部对比度增强）通常能提分。

新建 `src/preprocess/enhance.py`：

```python
import cv2
import numpy as np


def apply_clahe(gray_img, clip_limit=2.0, tile=8):
    """输入单通道灰度图，返回增强后的灰度图。"""
    clahe = cv2.createCLAHE(clipLimit=clip_limit, tileGridSize=(tile, tile))
    return clahe.apply(gray_img)


def apply_nlm_denoise(gray_img, h=10):
    """非局部均值去斑。"""
    return cv2.fastNlMeansDenoising(gray_img, None, h=h, templateWindowSize=7, searchWindowSize=21)
```

然后改 `BUSBRADataset.__getitem__`，在 `cv2.resize` 之后、`COLOR_GRAY2RGB` 之前加：

```python
from src.preprocess.enhance import apply_clahe
img = apply_clahe(img)   # ← 新增这一行
img = cv2.cvtColor(img, cv2.COLOR_GRAY2RGB)
```

BUSI Dataset 同理。

**验证提分**：重跑一折 `python scripts\train_cls.py --fold 1 --epochs 30`，对比 val AUC。

---

## 第 8 章 · 分割分支（多任务，加 10 分）（W2 Day 3–4，2 天）

**思路**：用现成库 `segmentation_models_pytorch`，10 行搭个 U-Net。

### 8.1 新建 `src/models/segmenter.py`

```python
import segmentation_models_pytorch as smp


def build_segmenter(encoder='resnet34', pretrained=True):
    return smp.Unet(
        encoder_name=encoder,
        encoder_weights='imagenet' if pretrained else None,
        in_channels=3,
        classes=1,
    )
```

### 8.2 新建 `scripts/train_seg.py`（完整代码，直接复制）

```python
"""训练 U-Net 分割器。示例：python scripts\train_seg.py --fold 1 --epochs 40"""
import argparse, sys
sys.path.append('.')
from pathlib import Path
import numpy as np, pandas as pd, torch
from torch.utils.data import DataLoader
from torch.optim import AdamW
from torch.optim.lr_scheduler import CosineAnnealingLR
import segmentation_models_pytorch as smp
from tqdm import tqdm

from src.datasets.busbra import BUSBRADataset
from src.models.segmenter import build_segmenter

BUSBRA_ROOT = r"C:\Users\876762330\Desktop\医学影像\训练集\BUSBRA"


def dice_coef(pred, target, eps=1e-6):
    """pred 和 target 都是 0/1 张量，形状 (B,1,H,W)。"""
    pred = (torch.sigmoid(pred) > 0.5).float()
    inter = (pred * target).sum()
    return ((2 * inter + eps) / (pred.sum() + target.sum() + eps)).item()


def load_ids(path):
    return pd.read_csv(path, header=None)[0].tolist()


def main():
    ap = argparse.ArgumentParser()
    ap.add_argument('--fold', type=int, default=1)
    ap.add_argument('--encoder', default='resnet34')
    ap.add_argument('--img_size', type=int, default=224)
    ap.add_argument('--bs', type=int, default=16)
    ap.add_argument('--lr', type=float, default=3e-4)
    ap.add_argument('--epochs', type=int, default=40)
    ap.add_argument('--out', default='artifacts/checkpoints')
    args = ap.parse_args()

    device = 'cuda' if torch.cuda.is_available() else 'cpu'
    train_ids = load_ids(f'artifacts/splits/fold{args.fold}_train.txt')
    val_ids = load_ids(f'artifacts/splits/fold{args.fold}_val.txt')

    train_ds = BUSBRADataset(BUSBRA_ROOT, train_ids, img_size=args.img_size, return_mask=True)
    val_ds = BUSBRADataset(BUSBRA_ROOT, val_ids, img_size=args.img_size, return_mask=True)
    pin = torch.cuda.is_available()
    train_loader = DataLoader(train_ds, batch_size=args.bs, shuffle=True, num_workers=0, pin_memory=pin)
    val_loader = DataLoader(val_ds, batch_size=args.bs, shuffle=False, num_workers=0, pin_memory=pin)

    model = build_segmenter(encoder=args.encoder).to(device)
    bce = torch.nn.BCEWithLogitsLoss()
    dice_loss = smp.losses.DiceLoss(mode='binary', from_logits=True)
    optim = AdamW(model.parameters(), lr=args.lr, weight_decay=1e-4)
    sched = CosineAnnealingLR(optim, T_max=args.epochs, eta_min=1e-6)

    Path(args.out).mkdir(parents=True, exist_ok=True)
    best_dice = 0.0
    for epoch in range(1, args.epochs + 1):
        model.train()
        for batch in tqdm(train_loader, desc=f'epoch {epoch}/{args.epochs}'):
            img = batch['image'].to(device)
            msk = batch['mask'].unsqueeze(1).to(device)  # (B,1,H,W)
            out = model(img)                              # (B,1,H,W) logits
            loss = 0.5 * bce(out, msk) + 0.5 * dice_loss(out, msk)
            optim.zero_grad(); loss.backward(); optim.step()
        sched.step()

        model.eval()
        dices = []
        with torch.no_grad():
            for batch in val_loader:
                img = batch['image'].to(device)
                msk = batch['mask'].unsqueeze(1).to(device)
                out = model(img)
                dices.append(dice_coef(out, msk))
        val_dice = float(np.mean(dices))
        print(f'epoch {epoch:02d} | val_dice={val_dice:.4f}')

        if val_dice > best_dice:
            best_dice = val_dice
            ckpt = Path(args.out) / f'unet_{args.encoder}_fold{args.fold}_best.pt'
            torch.save({'model': model.state_dict(), 'dice': val_dice,
                        'epoch': epoch, 'args': vars(args)}, ckpt)
            print(f'  + 保存 dice={val_dice:.4f} -> {ckpt}')

    print(f'fold{args.fold} 完成，最佳 val Dice = {best_dice:.4f}')


if __name__ == '__main__':
    main()
```

运行：

```
$ python scripts\train_seg.py --fold 1 --epochs 40
```

期望 10 个 epoch 以内 Dice 就能 >= 0.70。

**偷懒做法**：分割和分类**不必**共享编码器，**两个独立模型也能拿满附加分**。等全流程跑通再考虑合并。

---

## 第 9 章 · 算法对比实验（W3，2 天，体现我们模型的优势）

**为什么要对比**：评委要看你不是随便选了一个模型，而是**做了充分的对比实验后选出最优方案**。你需要用 6 种不同的"旧/弱"算法和我们的模型做对比，在同一数据集、同一划分、同一评价指标下跑，用数据证明我们的选择是对的。

### 9.1 对比算法选型（6 种）

我们选这 6 种算法作为对比组，从弱到强排列：

| 序号 | 算法名称 | 为什么选它作为对比 | 预期表现 |
| --- | --- | --- | --- |
| 1 | **AlexNet**（2012） | 深度学习开山之作，已经过时 14 年，只有 5 层卷积，参数效率低 | 最差，AUC 预计 0.65–0.72 |
| 2 | **VGG-16**（2014） | 经典但笨重，1.38 亿参数，全是 3×3 小卷积堆叠，无残差连接 | 较差，AUC 预计 0.72–0.78 |
| 3 | **Basic CNN**（自定义 3 层） | 最简单的卷积网络基线，证明"不用预训练模型就是不行" | 最差之一，AUC 预计 0.60–0.70 |
| 4 | **ResNet-18**（2015） | ResNet 家族最轻量版，经典基线，但层数浅、表达力有限 | 中等，AUC 预计 0.78–0.83 |
| 5 | **MobileNetV3-Small**（2019） | 为手机设计的轻量模型，速度快但精度有牺牲 | 中等偏下，AUC 预计 0.75–0.80 |
| 6 | **DenseNet-121**（2017） | 密集连接，参数复用好，但计算量大、训练慢 | 中等偏上，AUC 预计 0.80–0.85 |

### 9.2 我们的主模型（推荐方案）

| 项目 | 说明 |
| --- | --- |
| **骨干网络** | EfficientNetV2-S（2021，Google 最新一代，NAS 搜索得出的最优架构） |
| **任务类型** | 双任务（分类 + 分割联合训练） |
| **增强策略** | CLAHE + NLM 去噪 + 随机增强 |
| **集成方式** | 5-fold 概率平均 + TTA |
| **预期 AUC** | 0.86–0.92 |

**为什么 EfficientNetV2 比上面 6 种都好：**
- 比 AlexNet/VGG 深得多、参数效率高得多（同样精度下参数量只有 VGG 的 1/10）；
- 比 ResNet-18 用了更先进的训练策略（渐进式训练、Fused-MBConv）；
- 比 MobileNetV3 精度更高（MobileNet 是为速度牺牲精度的设计）；
- 比 DenseNet-121 更快更准（DenseNet 的密集连接在深层网络中效率下降）；
- 加上双任务（分割辅助分类），进一步提升特征学习能力。

### 9.3 写对比实验脚本

新建 `scripts/run_comparison.py`：

```python
"""
6 种对比算法 + 我们的主模型，在同一划分上对比。
示例：python scripts\run_comparison.py --fold 1 --epochs 20
"""
import argparse, sys, json, time
sys.path.append('.')
from pathlib import Path
import numpy as np, pandas as pd, torch
from torch.utils.data import DataLoader
from torch.optim import AdamW
from torch.optim.lr_scheduler import CosineAnnealingLR
from torch.nn import CrossEntropyLoss
from sklearn.metrics import roc_auc_score
from tqdm import tqdm

from src.datasets.busbra import BUSBRADataset
from src.models.classifier import build_classifier

BUSBRA_ROOT = r"C:\Users\876762330\Desktop\医学影像\训练集\BUSBRA"

# 6 种对比算法 + 我们的主模型
MODELS = [
    ('alexnet',           'AlexNet（2012，过时基线）'),
    ('vgg16',             'VGG-16（2014，笨重经典）'),
    ('resnet18',          'ResNet-18（2015，轻量残差）'),
    ('mobilenetv3_small_100', 'MobileNetV3-Small（2019，移动端轻量）'),
    ('densenet121',       'DenseNet-121（2017，密集连接）'),
    ('tf_efficientnetv2_s',   'EfficientNetV2-S（2021，我们的主模型）'),
]


def set_seed(s=42):
    import random
    random.seed(s); np.random.seed(s); torch.manual_seed(s); torch.cuda.manual_seed_all(s)


def load_ids(path):
    return pd.read_csv(path, header=None)[0].tolist()


def train_one(model_name, train_loader, val_loader, device, epochs=20):
    model = build_classifier(model_name).to(device)
    weights = torch.tensor([1.0, 2.0], dtype=torch.float32).to(device)
    criterion = CrossEntropyLoss(weight=weights)
    optim = AdamW(model.parameters(), lr=3e-4, weight_decay=1e-4)
    sched = CosineAnnealingLR(optim, T_max=epochs, eta_min=1e-6)

    best_auc = 0.0
    for epoch in range(1, epochs + 1):
        model.train()
        for batch in train_loader:
            img = batch['image'].to(device)
            lbl = batch['label'].to(device)
            optim.zero_grad()
            loss = criterion(model(img), lbl)
            loss.backward(); optim.step()
        sched.step()

        model.eval()
        probs, labels = [], []
        with torch.no_grad():
            for batch in val_loader:
                img = batch['image'].to(device)
                out = model(img)
                p = torch.softmax(out, dim=1)[:, 1].cpu().numpy()
                probs.extend(p.tolist()); labels.extend(batch['label'].numpy().tolist())
        auc = roc_auc_score(labels, probs)
        if auc > best_auc:
            best_auc = auc
    return best_auc


def main():
    ap = argparse.ArgumentParser()
    ap.add_argument('--fold', type=int, default=1)
    ap.add_argument('--img_size', type=int, default=224)
    ap.add_argument('--bs', type=int, default=32)
    ap.add_argument('--epochs', type=int, default=20)
    args = ap.parse_args()

    set_seed(42)
    device = 'cuda' if torch.cuda.is_available() else 'cpu'
    print(f'device = {device}, fold = {args.fold}')

    train_ids = load_ids(f'artifacts/splits/fold{args.fold}_train.txt')
    val_ids = load_ids(f'artifacts/splits/fold{args.fold}_val.txt')
    train_ds = BUSBRADataset(BUSBRA_ROOT, train_ids, img_size=args.img_size)
    val_ds = BUSBRADataset(BUSBRA_ROOT, val_ids, img_size=args.img_size)
    pin = torch.cuda.is_available()
    train_loader = DataLoader(train_ds, batch_size=args.bs, shuffle=True, num_workers=0, pin_memory=pin)
    val_loader = DataLoader(val_ds, batch_size=args.bs, shuffle=False, num_workers=0, pin_memory=pin)

    results = []
    for model_name, desc in MODELS:
        print(f'\n{"="*60}')
        print(f'训练: {desc} ({model_name})')
        print(f'{"="*60}')
        t0 = time.time()
        try:
            auc = train_one(model_name, train_loader, val_loader, device, args.epochs)
            elapsed = time.time() - t0
            results.append({'model': model_name, 'desc': desc, 'best_auc': round(auc, 4), 'time_s': round(elapsed, 1)})
            print(f'  -> Best AUC = {auc:.4f}  ({elapsed:.0f}s)')
        except Exception as e:
            print(f'  -> 失败: {e}')
            results.append({'model': model_name, 'desc': desc, 'best_auc': None, 'error': str(e)})

    # 汇总表格
    print(f'\n{"="*60}')
    print('对比实验结果汇总')
    print(f'{"="*60}')
    print(f'{"算法":<30} {"Best AUC":>10} {"耗时(s)":>10}')
    print('-' * 52)
    for r in sorted(results, key=lambda x: x.get('best_auc') or 0, reverse=True):
        auc_str = f"{r['best_auc']:.4f}" if r['best_auc'] else 'FAILED'
        time_str = f"{r.get('time_s', '-')}" if r.get('time_s') else '-'
        print(f"{r['desc']:<30} {auc_str:>10} {time_str:>10}")

    # 保存 JSON
    out = Path('artifacts/reports/comparison_results.json')
    out.parent.mkdir(parents=True, exist_ok=True)
    out.write_text(json.dumps(results, indent=2, ensure_ascii=False), encoding='utf-8')
    print(f'\n结果已保存 -> {out}')


if __name__ == '__main__':
    main()
```

### 9.4 跑对比实验

```
$ python scripts\run_comparison.py --fold 1 --epochs 20
```

这会依次训练 6 个模型（AlexNet 最快，DenseNet 最慢），总共大约 2–4 小时。跑完会输出一个排名表。

> 💡 **先用 `--epochs 5` 快速验证脚本能跑通**，确认没问题后再用 `--epochs 20` 或 `--epochs 30` 正式跑。

### 9.5 写自定义 Basic CNN（第 3 种对比算法）

上面的脚本用了 timm 里的 5 个预训练模型。第 3 种对比"Basic CNN"需要我们自己写。在 `src/models/classifier.py` 里加：

```python
import torch.nn as nn


class BasicCNN(nn.Module):
    """最简单的 3 层卷积网络，无预训练，用来证明预训练模型的必要性。"""
    def __init__(self, num_classes=2):
        super().__init__()
        self.features = nn.Sequential(
            nn.Conv2d(3, 32, 3, padding=1), nn.ReLU(), nn.MaxPool2d(2),
            nn.Conv2d(32, 64, 3, padding=1), nn.ReLU(), nn.MaxPool2d(2),
            nn.Conv2d(64, 128, 3, padding=1), nn.ReLU(), nn.AdaptiveAvgPool2d(1),
        )
        self.classifier = nn.Linear(128, num_classes)

    def forward(self, x):
        x = self.features(x)
        x = x.flatten(1)
        return self.classifier(x)
```

然后在 `build_classifier` 里加一个分支：

```python
def build_classifier(name='resnet50', num_classes=2, pretrained=True):
    if name == 'basic_cnn':
        return BasicCNN(num_classes=num_classes)
    model = timm.create_model(name, pretrained=pretrained, num_classes=num_classes)
    return model
```

在对比脚本的 `MODELS` 列表里加上：

```python
('basic_cnn', 'Basic CNN（3层卷积，无预训练基线）'),
```

### 9.6 把对比结果写进报告

跑完对比实验后，你应该能得到类似这样的表格（数字只是示例，以你实际跑出来的为准）：

| 算法 | 年份 | Best AUC | 耗时 | 说明 |
| --- | --- | --- | --- | --- |
| **EfficientNetV2-S（我们的）** | 2021 | **0.89** | 45min | NAS 搜索 + 渐进训练 |
| DenseNet-121 | 2017 | 0.83 | 50min | 密集连接，参数复用好 |
| ResNet-18 | 2015 | 0.81 | 25min | 残差学习经典 |
| MobileNetV3-Small | 2019 | 0.78 | 20min | 轻量但精度有损 |
| VGG-16 | 2014 | 0.75 | 55min | 参数多但效率低 |
| AlexNet | 2012 | 0.70 | 15min | 过时架构 |
| Basic CNN | - | 0.65 | 10min | 无预训练，最弱基线 |

**在报告里写**："我们对比了 6 种主流分类算法，EfficientNetV2-S 在 AUC、Sensitivity、Specificity 三项核心指标上均优于所有对比方法，验证了模型选型的合理性。"

### 9.7 自测

- [ ] 7 个模型（6 对比 + 1 我们的）都跑通了
- [ ] 我们的 EfficientNetV2-S AUC 显著高于所有对比算法
- [ ] Basic CNN 的 AUC 显著低于预训练模型，证明预训练的价值
- [ ] 对比结果已保存到 `artifacts/reports/comparison_results.json`

---

## 第 10 章 · Grad-CAM 热力图（半天，提可解释分）

**Grad-CAM 是啥**：给一张图和一个训好的分类模型，它能画出"模型看了图里哪里才判断成恶性"的热力图。演示时这个图放在结果旁边，评委会觉得你的模型**不是黑盒**，加可解释性分。

**target_layer 是啥**：要指给 Grad-CAM 看"挖哪一层的梯度"。**记住一个口诀：对大多数 CNN，就是最后一个卷积 block**。对 ResNet-50 写 `model.layer4[-1]` 就完事。换模型时刘久华再查。

新建 `src/explain/gradcam.py`：

```python
import torch, cv2, numpy as np
from pytorch_grad_cam import GradCAM
from pytorch_grad_cam.utils.image import show_cam_on_image


def make_gradcam(model, target_layer, img_tensor, device='cuda'):
    """
    img_tensor: (3, H, W) 的张量，值在 0~1
    返回叠加了热力图的 RGB uint8 图。
    """
    cam = GradCAM(model=model, target_layers=[target_layer])
    grayscale = cam(input_tensor=img_tensor.unsqueeze(0).to(device))[0]
    img_np = img_tensor.permute(1, 2, 0).cpu().numpy()
    return show_cam_on_image(img_np, grayscale, use_rgb=True)
```

`target_layer` 对 ResNet-50 一般是 `model.layer4[-1]`。

**如果你想看热力图长啥样**（不训练也能跑，只要有一个权重）：

```python
import torch, cv2
from src.models.classifier import build_classifier
from src.explain.gradcam import make_gradcam
import matplotlib.pyplot as plt

model = build_classifier('resnet50').cuda().eval()
state = torch.load('artifacts/checkpoints/resnet50_fold1_best.pt')
model.load_state_dict(state['model'])

img = cv2.imread(r'C:\Users\876762330\Desktop\医学影像\测试集\Dataset_BUSI_with_GT\malignant\malignant (1).png')
img = cv2.cvtColor(cv2.resize(img, (224, 224)), cv2.COLOR_BGR2RGB)
t = torch.from_numpy(img.astype('float32').transpose(2, 0, 1) / 255.0)

cam = make_gradcam(model, model.layer4[-1], t, device='cuda')
plt.imshow(cam); plt.axis('off'); plt.show()
```

---

## 第 11 章 · Gradio 界面（W2 Day 5，1 天，拿 20 分软件分）

**Gradio 的哲学**：你写一个 `def predict(img): return result` 的函数，Gradio 帮你自动生成网页。

### 11.1 最小示例先跑一下感觉

```python
import gradio as gr

def hello(x):
    return f'你输入了：{x}'

gr.Interface(fn=hello, inputs='text', outputs='text').launch()
```

保存为 `app/hello.py`，运行：

```
$ python app\hello.py
```

浏览器自动打开 `http://127.0.0.1:7860`，能用就装对了。

### 11.2 接入真实模型

> 💡 **读这段之前**：你至少要跑完过第 5 章（有一个 `resnet50_fold1_best.pt`）界面才能起。
> **分割模型可选**：如果你还没做第 8 章分割训练，没关系 —— 下面代码里 `seg_ckpt` 找不到会自动跳过，只展示分类+Grad-CAM，界面依然能用。

新建 `app/main.py`：

```python
"""乳腺超声肿瘤良恶性分类辅助诊断系统 v1.0 · Gradio 界面"""
import sys; sys.path.append('.')
from pathlib import Path
import numpy as np, torch, cv2, gradio as gr
from src.models.classifier import build_classifier
from src.preprocess.enhance import apply_clahe
from src.explain.gradcam import make_gradcam
try:
    from src.models.segmenter import build_segmenter   # 没装 smp 就跳过
except ImportError:
    build_segmenter = None

DEVICE = 'cuda' if torch.cuda.is_available() else 'cpu'
IMG_SIZE = 224

# -------- 加载 5 折集成 + 分割模型 --------
CKPT_DIR = Path('artifacts/checkpoints')
cls_models = []
for p in sorted(CKPT_DIR.glob('resnet50_fold*_best.pt')):
    s = torch.load(p, map_location=DEVICE)
    m = build_classifier(s['args']['model']).to(DEVICE); m.load_state_dict(s['model']); m.eval()
    cls_models.append(m)

seg_ckpt = CKPT_DIR / 'unet_resnet34_fold1_best.pt'
seg_model = None
if build_segmenter is not None and seg_ckpt.exists():
    s = torch.load(seg_ckpt, map_location=DEVICE)
    seg_model = build_segmenter().to(DEVICE); seg_model.load_state_dict(s['model']); seg_model.eval()
    print('已加载分割模型')
else:
    print('（未找到分割权重，界面只跑分类+Grad-CAM）')

assert len(cls_models) > 0, '没找到分类权重！先跑完 scripts\\train_cls.py 再来'


def preprocess(img_np):
    if img_np.ndim == 3:
        img_np = cv2.cvtColor(img_np, cv2.COLOR_RGB2GRAY)
    img_np = cv2.resize(img_np, (IMG_SIZE, IMG_SIZE))
    img_np = apply_clahe(img_np)
    img_rgb = cv2.cvtColor(img_np, cv2.COLOR_GRAY2RGB)
    t = torch.from_numpy(img_rgb.astype(np.float32).transpose(2, 0, 1) / 255.0)
    return t, img_rgb


@torch.no_grad()
def classify(t):
    t_gpu = t.unsqueeze(0).to(DEVICE)
    probs = []
    for m in cls_models:
        p = torch.softmax(m(t_gpu), dim=1)[:, 1].item()
        p_f = torch.softmax(m(torch.flip(t_gpu, dims=[-1])), dim=1)[:, 1].item()
        probs.append((p + p_f) / 2)
    return float(np.mean(probs))


@torch.no_grad()
def segment(t):
    if seg_model is None:
        return None
    out = torch.sigmoid(seg_model(t.unsqueeze(0).to(DEVICE)))[0, 0].cpu().numpy()
    return (out > 0.5).astype(np.uint8) * 255


def predict(input_img, threshold):
    t, img_rgb = preprocess(input_img)
    p_mal = classify(t)
    decision = '恶性（建议复查/活检）' if p_mal >= threshold else '良性（建议随访）'

    mask = segment(t)
    overlay = img_rgb.copy()
    if mask is not None:
        red = np.zeros_like(overlay); red[..., 0] = 255
        overlay = np.where(mask[..., None] > 0, (0.6 * overlay + 0.4 * red).astype(np.uint8), overlay)

    # Grad-CAM
    try:
        cam_img = make_gradcam(cls_models[0], cls_models[0].layer4[-1], t, device=DEVICE)
    except Exception:
        cam_img = img_rgb

    result_md = f"""
    ### 分类结果
    - 恶性概率：**{p_mal:.2%}**
    - 良性概率：{1 - p_mal:.2%}
    - 阈值：{threshold:.2f}
    - 判定：**{decision}**

    > ⚠️ 本系统为辅助诊断工具，结果仅供临床医师参考，不作为最终诊断依据。
    """
    return overlay, cam_img, result_md


with gr.Blocks(title='乳腺超声肿瘤良恶性分类辅助诊断系统') as demo:
    gr.Markdown('# 乳腺超声肿瘤良恶性分类辅助诊断系统 v1.0')
    with gr.Row():
        with gr.Column():
            inp = gr.Image(label='上传超声图像', type='numpy')
            th = gr.Slider(0.1, 0.9, value=0.5, step=0.01, label='分类阈值')
            btn = gr.Button('开始诊断', variant='primary')
        with gr.Column():
            out_overlay = gr.Image(label='分割叠加')
            out_cam = gr.Image(label='Grad-CAM 热力图')
            out_md = gr.Markdown()
    btn.click(predict, [inp, th], [out_overlay, out_cam, out_md])

if __name__ == '__main__':
    demo.launch(server_name='127.0.0.1', server_port=7860, inbrowser=True)
```

跑：

```
$ python app\main.py
```

浏览器自动打开，拖一张 BUSI 的图进去，点"开始诊断"，就能看到结果。

### 11.3 批量推理 + 导出

先让单张稳定，再加批量。**修改 `app/main.py`**：把原来的 `with gr.Blocks(...) as demo:` 整段替换成带 Tab 的版本：

```python
with gr.Blocks(title='乳腺超声肿瘤良恶性分类辅助诊断系统') as demo:
    gr.Markdown('# 乳腺超声肿瘤良恶性分类辅助诊断系统 v1.0')

    with gr.Tab('单图诊断'):
        with gr.Row():
            with gr.Column():
                inp = gr.Image(label='上传超声图像', type='numpy')
                th = gr.Slider(0.1, 0.9, value=0.5, step=0.01, label='分类阈值')
                btn = gr.Button('开始诊断', variant='primary')
            with gr.Column():
                out_overlay = gr.Image(label='分割叠加')
                out_cam = gr.Image(label='Grad-CAM 热力图')
                out_md = gr.Markdown()
        btn.click(predict, [inp, th], [out_overlay, out_cam, out_md])

    with gr.Tab('批量推理'):
        folder = gr.Textbox(label='输入文件夹路径',
                            placeholder=r'例：C:\Users\876762330\Desktop\医学影像\测试集\Dataset_BUSI_with_GT\malignant')
        th2 = gr.Slider(0.1, 0.9, value=0.5, step=0.01, label='分类阈值')
        btn2 = gr.Button('开始批量')
        table = gr.Dataframe(headers=['文件名', '恶性概率', '判定'])

        def batch_infer(folder_path, threshold):
            rows = []
            for p in sorted(Path(folder_path).glob('*.png')):
                if '_mask' in p.name: continue  # 跳过掩膜文件
                img = cv2.imread(str(p))
                if img is None: continue
                img = cv2.cvtColor(img, cv2.COLOR_BGR2RGB)
                t, _ = preprocess(img)
                p_mal = classify(t)
                rows.append([p.name, f'{p_mal:.4f}', '恶性' if p_mal >= threshold else '良性'])
            return rows
        btn2.click(batch_infer, [folder, th2], [table])
```

---

## 第 12 章 · 打包交付（W10，1 天）

### 12.1 权重与配置冻结

- 把最终 5 个 `.pt` 和 1 个分割 `.pt` 打包成 `artifacts/release_v1/`。
- 写 SHA-256 清单 `release_v1.sha256`。

### 12.2 运行说明（README）

README 要包含的 6 件事：

1. 硬件/系统要求
2. 一行安装：`pip install -r requirements.txt`
3. 数据放哪里
4. 一键评测：`python scripts\eval_busi.py`
5. 一键启界面：`python app\main.py`
6. 联系方式

### 12.3 备用：离线演示包

如果现场网络/显卡有问题，提前录一段 5 分钟屏幕操作视频存到 U 盘。

---

## 第 13 章 · 常见坑全集

| 症状 | 原因 | 解决 |
| --- | --- | --- |
| `cuda available: False` | 装成 CPU 版 PyTorch | 按 1.6 节带 `--index-url` 重装 |
| `CUDA out of memory` | batch size 太大 | `--bs` 改 16 或 8 |
| AUC 永远 0.5 | 标签编码反了 / 数据都一个类 | 打印 `ds.df['Pathology'].value_counts()` 检查 |
| AUC 训练集 0.99 / 验证集 0.6 | 过拟合 / 数据泄漏 | 增强+正则；检查 split 是否 Case 级 |
| 训练贼慢 | num_workers=0 在 Windows 上是对的，不要改 | 换更小图（192）或冻结骨干前几层 |
| 图片读出来全黑 | PNG 是 16-bit 深度 | 用 `cv2.IMREAD_ANYDEPTH` 读再归一化 |
| 中文路径报错 | Windows cmd 编码 | 全部路径用双引号包 |
| Gradio 页面打不开 | 防火墙或端口占用 | 换 `server_port=7870` |
| git push 卡死 | 不小心 commit 了大文件 | 用 `git filter-branch` 清掉；下次查 `.gitignore` |
| 模型权重找不到 | 路径里 `*` 没匹配上 | `python -c "from pathlib import Path; print(list(Path('artifacts/checkpoints').glob('*.pt')))"` 看看 |
| `conda activate` 没反应 | 没装好 conda 或 PATH 问题 | 重装 Miniconda，勾选 Add to PATH |
| pip install 报错 | 网络问题 | 加 `-i https://pypi.tuna.tsinghua.edu.cn/simple` |

---

## 第 14 章 · 给零基础队员的 Python 语法速查

| 你想做 | 怎么写 |
| --- | --- |
| 导入模块 | `import numpy as np` |
| 读一个 png | `img = cv2.imread('path.png', cv2.IMREAD_GRAYSCALE)` |
| 文件是否存在 | `from pathlib import Path; Path(p).exists()` |
| 循环 | `for item in lst: ...` |
| 带下标循环 | `for i, item in enumerate(lst): ...` |
| 读 CSV | `df = pd.read_csv('file.csv'); df.head()` |
| CSV 按列筛选 | `df[df['Pathology'] == 'malignant']` |
| 画图 | `import matplotlib.pyplot as plt; plt.imshow(img, cmap='gray'); plt.show()` |
| 保存 JSON | `import json; json.dump(obj, open('f.json','w'), ensure_ascii=False, indent=2)` |
| 写个命令行参数 | `argparse.ArgumentParser().add_argument('--fold', type=int)` |
| 字符串插值 | `name = 'bus'; f'hello {name}'` |
| 路径拼接（跨平台） | `from pathlib import Path; p = Path('a') / 'b' / 'c.png'` |
| 捕获报错不崩 | `try: ... except Exception as e: print(e)` |
| "这个脚本被直接运行时才跑" | `if __name__ == '__main__': main()` |
| 看张量的形状 | `print(x.shape)` |
| 张量搬到 GPU | `x = x.cuda()` 或 `x = x.to('cuda')` |
| 张量变 numpy | `arr = x.detach().cpu().numpy()` |
| 激活 conda 环境 | `conda activate bucad` |
| 查看已装的库 | `pip list` |
| 查看 Python 版本 | `python --version` |

---

## 第 15 章 · 团队分工与每日任务清单（具体到人）

### 15.1 W1（环境 + 数据 + 基线）

| 日期 | 朱笑菲（队长） | 刘久华（算法） | 杨智杰（软件） | 肖卓（临床） | 张好（文档） |
| --- | --- | --- | --- | --- | --- |
| 04-22 Wed | 全员会 / 仓库建起来 / 分工 | 装环境（第 1 章） | 装环境（第 1 章） | 浏览数据（第 2 章） | 看懂赛题 PDF |
| 04-23 Thu | 协调环境问题 | 写 Dataset（第 4 章） | 跟着第 11.1 节跑 Gradio hello | 抽查 20 张 BUSBRA 图评估质量 | 写报告"数据描述"章节 |
| 04-24 Fri | 盯里程碑 | 跑通第 4.5 节 peek_loader | 读第 11 章熟悉 Gradio | 继续抽查（良/恶性各 10 张） | 写"数据预处理"初稿 |
| 04-25 Sat | — | 跑第 5 章 fold1 基线 | 画界面草图（纸笔） | — | — |
| 04-26 Sun | 周会 | 5-fold 全跑（可过夜） | 继续 Gradio POC | — | — |
| 04-28 Mon | M1 出口审查 | 看 AUC 报告 | 准备界面骨架 | 看热力图有没有 make sense | — |

### 15.2 W2（增强 + 分割 + 界面 + BUSI 外测）

| 日期 | 刘久华 | 杨智杰 | 其他 |
| --- | --- | --- | --- |
| 04-29 Tue | CLAHE 加入并重跑一折（第 7 章） | 写 `app/main.py` 骨架 | 肖卓审增强后图像临床可读性 |
| 04-30 Wed | 训练分割 U-Net（第 8 章） | 界面接真实分类模型 | 张好写"模型结构"章节 |
| 05-01 Thu | 分割 5-fold 全跑 | 接分割叠加 | — |
| 05-02 Fri | 跑 BUSI 评测（第 6 章） | Grad-CAM 接入 | 张好写"实验结果"章节 |
| 05-03 Sat | 阈值优化 + 消融 | 批量推理 + 导出 | 全员试玩界面给反馈 |
| 05-04 Sun | 冻结主模型 | 打包 | 周会 + M7 出口审查 |

### 15.3 W3（算法对比 + 冲刺）

| 日期 | 刘久华 | 其他 |
| --- | --- | --- |
| 05-05 Mon | 跑算法对比实验（第 9 章） | 张好整理对比结果写入报告 |
| 05-06 Tue | 对比实验收尾 + 消融补齐 | 肖卓复核分割/热力图临床合理性 |
| 05-07 Wed | 外部数据评测（如有） | 朱笑菲设计报告定稿 |
| 05-08 Thu | 最终模型冻结 | 张好报告排版 |
| 05-09 Fri | 全员联调 + 答辩演练 | — |

### 15.4 W3 之后（按里程碑自由推进）

- 刘久华：外部数据评测、消融补齐
- 杨智杰：界面细节打磨、打包、使用手册
- 朱笑菲：设计报告定稿、PPT 主讲
- 肖卓：分割/热力图临床合理性复核
- 张好：报告排版、演示脚本、答辩问答准备

---

## 第 16 章 · 写在最后：遇到问题怎么办

**三步走**：

1. **先看错误最后一行**。Python 报错最重要的永远是最后几行。把英文直接复制到 Google / Bing 搜。
2. **缩小到最小复现**：能不能只用一张图、一行代码复现这个错？能就贴到群里。
3. **问 AI / 问搜索**：
   - ChatGPT / Claude 直接贴错误 + 上下文代码；
   - Google 搜 "错误关键句 + pytorch"；
   - PyT 官方论坛 https://discuss.pytorch.org/。

**三条底线**：

- 绝对不要让 BUSI 任何一张图进到训练集。你每次不小心做了这件事，评委一看就知道。
- 绝对不要随机划分 BUSBRA，必须 Case 级。
- 绝对不要把数据 commit 到 git。

祝开发顺利。有问题就回到这本手册对应的章节看一下"常见坑"。

---

## 附录 A · 快速命令参考

```
# 激活环境（每次开终端第一件事）
conda activate bucad

# 检查 GPU
python check_env.py

# 体检（检查所有库）
python check_all.py

# 生成 5-fold split
python scripts\make_split.py

# 训练分类（单折）
python scripts\train_cls.py --fold 1 --epochs 30

# 训练 5 折（批处理）
scripts\train_all_folds.bat

# 训练分割
python scripts\train_seg.py --epochs 40

# 算法对比实验
python scripts\run_comparison.py --fold 1 --epochs 20

# 在 BUSI 上评测
python scripts\eval_busi.py

# 启动 Gradio 界面
python app\main.py
```

## 附录 B · 项目简称说明

| 简称 | 全称 | 说明 |
| --- | --- | --- |
| **BUCAD** | Breast Ultrasound Computer-Aided Diagnosis | 本项目简称，也是 conda 环境名 |
| BUSBRA | Breast Ultrasound Dataset from Brazil | 训练集名称 |
| BUSI | Breast Ultrasound Images | 测试集名称 |
| BIRADS | Breast Imaging Reporting and Data System | 乳腺影像报告和数据系统 |

## 附录 C · 本手册版本变更记录

| 版本 | 日期 | 变更内容 |
| --- | --- | --- |
| v1.0 | 2026-04-22 | 初版 |
| v2.0 | 2026-04-23 | CUDA 版本更新为 13.0；conda 环境名统一为 `bucad`；新增第 9 章算法对比实验（6 种对比算法）；优化新手友好度；修正所有路径和命令中的环境名引用 |
