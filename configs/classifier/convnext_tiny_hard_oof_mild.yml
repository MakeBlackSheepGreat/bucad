paths_config: configs/paths.local.yml
task_type: classification
seed: 42
device: auto
model:
  name: convnext_tiny
  pretrained: true
  in_chans: 3
  num_classes: 2
data:
  image_size: 224
  batch_size: 8
  num_workers: 0
  preprocess:
    clahe: true
    use_timm_data_config: true
  augmentation:
    horizontal_flip: true
    flip_probability: 0.5
training:
  epochs: 30
  learning_rate: 0.00005
  weight_decay: 0.05
  label_smoothing: 0.02
  loss: cross_entropy
  class_weights: balanced
  sample_weight_path: ./artifacts/reports/busbra_oof_hard_sample_weights_mild.csv
  sample_weight_column: sample_weight
  checkpoint_strategy: best_auc
  scheduler:
    name: warmup_cosine
    warmup_epochs: 3
    min_lr: 0.000001
  fold_count: 5
  split_path: ./artifacts/reports/busbra_5fold_splits.csv
  positive_class: malignant
output:
  checkpoint_name: convnext_tiny_hard_oof_mild_fold{fold}.pt
  report_name: train_cls_convnext_tiny_hard_oof_mild_fold{fold}.json
